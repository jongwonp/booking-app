// src/lib/admin.ts
// Week4 라이트 버전: 일단은 무조건 통과시키고,
// 나중에 여기에서 세션/권한 검사 넣을 예정.

export async function assertAdmin() {
  // TODO: 나중에 iron-session 등으로 세션을 읽고
  // 관리자 권한이 아니면 에러를 던지거나 리다이렉트할 예정.
  return;
}
